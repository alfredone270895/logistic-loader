/**
 * modifico posizione del cubo
 * @param cube
 * @param x
 * @param y
 * @param z
 */
export function setCubePosition(cube, x, y, z) {
  cube.position.x = x;
  cube.position.y = y;
  cube.position.z = z;
}
